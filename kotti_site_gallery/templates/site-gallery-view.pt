<!DOCTYPE html>
<html xmlns="http://www.w3.org/1999/xhtml"
      xml:lang="en"
      xmlns:tal="http://xml.zope.org/namespaces/tal"
      xmlns:i18n="http://xml.zope.org/namespaces/i18n"
      i18n:domain="kotti_site_gallery"
      metal:use-macro="api.macro('kotti:templates/view/master.pt')">
  <article metal:fill-slot="content" class="site-gallery-view content">
    <h1>${api.context.title}</h1>
    <p class="description">
      ${api.context.description}
    </p>
    <h2 i18n:translate="">Sites</h2>
    <ul tal:condition="sites" class="site-list">
      <li tal:repeat="site sites">
				<div class="site-icon" tal:define="icon site.get_icon()">
					<a tal:condition="icon != None" href="${api.url(site)}" title="${site.description}"><img src="${api.url(icon, '@@icon-view')}"/></a>
				</div>
				<br />
        <a href="${api.url(site)}" title="${site.description}">${site.title}</a>
      </li>
    </ul>
  </article>
</html>
